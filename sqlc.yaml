version: "2"
sql:
  - schema: "migration/"
    queries: "internal/wallet/repository/queries.sql"
    gen:
      go:
        package: "repository"
        out: "internal/wallet/repository"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
